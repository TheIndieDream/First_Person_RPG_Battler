using UnityEngine;

[CreateAssetMenu(fileName = "new Player Camera Command Stream",
    menuName = "Command Stream.../Player Camera Command Stream")]
public class PlayerCameraCommandStream : CommandStream<Command<CinemachineFirstPersonCamera>>
{
    
}
